# Phase 3 Synthetic Harness Boundary

Phase 3 reuses the reviewed synthetic inputs under `fixtures/phase-1/`; it does not duplicate or
modify them. Committed Phase 3 fixture artifacts may contain only safe expected counts or report
shapes. Generated SQLite databases, authoritative-import reports, restricted quarantine payloads,
and temporary MySQL data must remain under ignored local storage and must not be committed.
