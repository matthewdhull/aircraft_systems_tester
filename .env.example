# Safe local defaults only. Never place credentials or production data here.
APP_ENV=development
DATABASE_PATH=.runtime/aircraft-systems-tester.sqlite
LOG_LEVEL=info
HOST=127.0.0.1
PORT=3000
ORIGIN=http://127.0.0.1:3000
SHUTDOWN_TIMEOUT=10
# Generate unique synthetic local values; never commit production key material.
GENERATION_SEED_KEY_BASE64=
GENERATION_SEED_KEY_ID=
GENERATION_CODE_HMAC_KEY_BASE64=
